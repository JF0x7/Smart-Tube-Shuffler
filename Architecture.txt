   ┌──────────────────────────────────────────────────────────┐
   │                 SMARTTUBE CONTROLLER                     │
   └──────────────────────────────────────────────────────────┘
                 |                 |                 |
                 v                 v                 v
        host (videos)     apiPort (queue)      token (history)
                 |                 |                 |
                 |                 |                 |
                 v                 v                 v
   ┌──────────────────────────────────────────────────────────┐
   │               MILLENNIUM BOX SHUFFLER                    │
   └──────────────────────────────────────────────────────────┘

   ┌──────────────────────────────────────────────────────────┐
   │  deck (cards)        ←─────────────── host               │
   │  hand (drawn)        ←─────────────── apiPort            │
   │  graveyard (used)    ←─────────────── token              │
   └──────────────────────────────────────────────────────────┘

   ┌──────────────────────────────────────────────────────────┐
   │                    METHOD MAPPING                        │
   └──────────────────────────────────────────────────────────┘

   SmartTube Method                     Shuffler Equivalent
   ───────────────────────────────────────────────────────────
   connectAPIAndPairIfNeeded()   →   loadDefaultDeck()
   refreshAll()                  →   shuffleDeck()
   refreshFast()                 →   cutDeck()        (optional)
   togglePlay()                  →   drawCard()
   play()                        →   dealToTable()    (future)
   next()                        →   discardSelected()
   previous()                    →   returnHandToDeck()
   clearQueue()                  →   clearTable()
   search()                      →   searchCards()
   clearSearchResults()          →   clearSearch()
   copyLogs()                    →   exportLogs()
   copyDiagnostics()             →   getStatistics()

   ┌──────────────────────────────────────────────────────────┐
   │                     STATE FLOW                           │
   └──────────────────────────────────────────────────────────┘

                 ┌──────────────┐
                 │   baseDeck    │
                 └──────┬───────┘
                        │ load
                        v
                 ┌──────────────┐
                 │    deck       │
                 └──────┬───────┘
                        │ shuffle / draw
                        v
                 ┌──────────────┐
                 │     hand      │
                 └──────┬───────┘
                        │ discard
                        v
                 ┌──────────────┐
                 │  graveyard    │
                 └──────────────┘

   ┌──────────────────────────────────────────────────────────┐
   │                 MILLENNIUM BOX ENERGY FLOW               │
   └──────────────────────────────────────────────────────────┘

      [Base Deck] → [Deck Grid] → [Hand Tiles] → [Graveyard Tile]
           |            |             |               |
           |            |             |               |
           └──────→  Millennium Eye Logic  ←─────────┘
